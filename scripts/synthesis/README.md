# RTL synthesis scripts

Place the synthesis entry point and timing constraints here after the synthesis tool,
technology library, PVT corner, and comparison constraints are fixed.

Expected files include `synth.tcl` and `constraints.tcl`. They are intentionally not created
as nonfunctional placeholders because synthesis provenance is part of the comparison
contract.
